<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:label="fly_player"
        android:name=".FlyPlayerApplication"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config">
        <meta-data
            android:name="miui.supportAppContinuity"
            android:value="true" />
        <meta-data
            android:name="android.supports_size_changes"
            android:value="true" />
        <!-- 保留 Impeller-Vulkan：真机实测 Adreno/Mali 两台设备稳态 80–172fps、菜单合成顺滑。
             不要加 ImpellerBackend=opengles（GLES 合成器在本机很慢，~27fps）。
             低帧仅出现在启动阶段（载入 + AI 模型冷启动），靠预热 AI 模型解决，与渲染后端无关。
             注：曾测 EnableImpeller=false(Skia 回退)，Mali 上仅略好、且 buffer-stall/raster
             尖刺依旧（甚至更糟），证实卡顿不在 Flutter 渲染后端，而在视频解码缓冲路径
             （hwdec 的 mali_gralloc 格式不匹配）——见 VideoOutputController 的 Mali 分支。 -->
        <meta-data
            android:name="io.flutter.embedding.android.EnableImpeller"
            android:value="true" />
        <meta-data
            android:name="embedded"
            android:value="true" />
        <!-- 原生播放壳截图分享用：把缓存/外部目录的截图文件以 content uri 授权给分享目标。 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.nativeplayer.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/native_player_file_paths" />
        </provider>
        <property
            android:name="android.window.PROPERTY_ACTIVITY_EMBEDDING_SPLITS_ENABLED"
            android:value="true" />
        <property
            android:name="android.window.PROPERTY_ACTIVITY_EMBEDDING_ALLOW_SYSTEM_OVERRIDE"
            android:value="false" />
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_ORIENTATION_OVERRIDE"
            android:value="false" />
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_DISPLAY_ORIENTATION_OVERRIDE"
            android:value="false" />
        <property
            android:name="android.window.PROPERTY_COMPAT_IGNORE_REQUESTED_ORIENTATION"
            android:value="false" />
        <property
            android:name="android.window.PROPERTY_COMPAT_ALLOW_IGNORING_ORIENTATION_REQUEST_WHEN_LOOP_DETECTED"
            android:value="false" />
        <activity
            android:name=".DetailActivity"
            android:exported="false"
            android:taskAffinity="com.geqian.flyplayer.fly_player"
            android:theme="@style/DetailPaneLaunchTheme"
            android:resizeableActivity="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/DetailPaneNormalTheme" />
        </activity>
        <activity
            android:name=".PlaceholderActivity"
            android:exported="false"
            android:taskAffinity="com.geqian.flyplayer.fly_player"
            android:theme="@style/ParallelPaneLaunchTheme"
            android:resizeableActivity="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/ParallelPaneNormalTheme" />
        </activity>
        <activity
            android:name=".HomePaneActivity"
            android:exported="false"
            android:taskAffinity="com.geqian.flyplayer.fly_player"
            android:theme="@style/LaunchTheme"
            android:resizeableActivity="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
        </activity>
        <activity
            android:name=".ExternalLocalVideoActivity"
            android:exported="true"
            android:theme="@style/PlayerLaunchTheme"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:resizeableActivity="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="video/*" />
                <data android:scheme="file" android:mimeType="video/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" android:mimeType="application/octet-stream" />
                <data android:scheme="file" android:mimeType="application/octet-stream" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:scheme="content" />
                <data android:scheme="file" />
            </intent-filter>
        </activity>
        <activity
            android:name=".NativePlayerActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity="com.geqian.flyplayer.fly_player.nativeplayer"
            android:theme="@style/PlayerLaunchTheme"
            android:supportsPictureInPicture="true"
            android:resizeableActivity="true"
            android:autoRemoveFromRecents="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true" />
        <activity
            android:name=".FullscreenScreenshotActivity"
            android:exported="false"
            android:taskAffinity="com.geqian.flyplayer.fly_player"
            android:theme="@style/PlayerLaunchTheme"
            android:resizeableActivity="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/PlayerNormalTheme" />
        </activity>
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:taskAffinity="com.geqian.flyplayer.fly_player"
            android:theme="@style/LaunchTheme"
            android:resizeableActivity="true"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />

        <service
            android:name=".PlayerNotificationService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="false" />

        <!-- 原生播放壳专用前台播放服务（锁屏/通知/蓝牙线控直达 playerSurface，不经 Flutter）。 -->
        <service
            android:name=".NativePlaybackMediaService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback"
            android:stopWithTask="true" />

        <receiver
            android:name=".PlaybackCommandReceiver"
            android:enabled="true"
            android:exported="false" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
